apiVersion: apps/v1
kind: Deployment
metadata:
  name: mock-ec-prf
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    spec:
      restartPolicy: Always
      imagePullSecrets:
        - name: regcred
      containers:
        - name: mock-ec-prf
          image: mock-ec-prf-image
          imagePullPolicy: Always
          ports:
            - name: mock-ec-prf
              containerPort: 8484
              protocol: TCP
          env:
            - name: sleepEnabled
              value: true
          resources:
            requests:
              memory: 2Gi
              cpu: 500m
            limits:
              memory: 3Gi
              cpu: 800m